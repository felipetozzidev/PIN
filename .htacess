# Ativa o motor de reescrita de URL do Apache
RewriteEngine On

# Define a base da URL para as regras de reescrita. 
# Isso ajuda a evitar problemas em subdiretórios.
RewriteBase /PIN2/PIN_Testes/

# CONDIÇÕES PARA A REESCRITA
# A regra abaixo só será aplicada se as seguintes condições forem verdadeiras:

# Condição 1: Não aplica a regra se a URL corresponder a um arquivo existente.
# O '-f' significa "é um arquivo". O '!' significa "não".
RewriteCond %{REQUEST_FILENAME} !-f

# Condição 2: Não aplica a regra se a URL corresponder a um diretório existente.
# O '-d' significa "é um diretório".
RewriteCond %{REQUEST_FILENAME} !-d

# REGRA DE REESCRITA (Exemplo)
# Se você tiver uma regra para centralizar tudo em um index.php, ela viria aqui.
# Se você não usa URLs amigáveis, pode até deixar sem nenhuma RewriteRule por enquanto.
# Exemplo de regra que pode existir: RewriteRule . index.php [L]

